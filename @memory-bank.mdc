# @memory-bank.mdc

## Initial Sequence Planning (2026-02-04)

### 統合完了項目
- [x] タスク整理プランを作成（`INITIAL_SEQUENCE_PLAN_JP.md`）。
- [x] 今週の優先順位を整理（ビルド復旧 → 低リスク修正 → テスト調査）。

### 未完了・検討事項
- [ ] `packages/shared-utils` 追加による境界問題解消。
- [ ] 型注釈追加・未使用変数修正。
- [ ] テスト失敗原因の調査と修正。
- [ ] PR/Release 情報の最新化（PR #148/#206/#167, Issue #207）。

### 次回セッション引き継ぎ
- 今週の実行順に沿って P1 作業から着手。
- 実行時に PR/Issue の最新状況を確認。

## P1実行アップデート (2026-02-04)

### 完了項目
- Task Tool関連実装を `packages/coding-agents` 配下へ移設。
- 旧パス `scripts/operations/*` と `utils/*` は再エクスポートで互換維持。
- `packages/coding-agents` に `@miyabi/shared-utils` 依存を追加。
- 型注釈/型明示の軽微修正を実施。
- 最小検証: `packages/coding-agents` の `tsc` と task-grouper テスト通過。

### 未完了・次のアクション
- 残りのテスト失敗調査（integration系など）。
- 未使用変数修正の確認（`review-agent.ts` 等）。
- PR/Release 情報の最新化（P2）。

## CLI E2E修正 (2026-02-04)

### 完了項目
- `run --task --json` がJSONモードとして扱われるよう修正（`isJsonMode()` を利用）。
- `--yes` ヘルプ文言から `prompt` を除去。
- `e2e-cli.test.ts` の失敗2件は再実行で解消。

### 検証
- `npm run typecheck` ✅
- `npx vitest packages/cli/src/__tests__/e2e-cli.test.ts` ✅

## Full Test Status (2026-02-04)

### 検証結果
- `npm test` ✅
- Test Files: 50 passed, 3 skipped
- Tests: 709 passed, 62 skipped

### 備考
- Tailwind `content` 設定の警告あり（テスト自体は成功）。
- ReviewAgent の coverage未検出警告あり（テスト自体は成功）。

## Codex Repo Config Bootstrap (2026-03-20)

### 完了項目
- `.codex/config.toml` を新規作成し、Miyabi 向け project-scoped Codex 設定を追加。
- `.codex/agents/*.toml` を追加し、default/explorer/reviewer/worker/architect の role config を分離。
- `.codex/hooks/notify.py` と `.codex/hooks/session-end.sh` を追加し、`agent-turn-complete` 通知と `.ai/logs` 追記を実装。
- `.codex/README.md` と `.codex/hooks/README.md` を追加。

### 次回セッション向けメモ
- Codex でこの repo を trusted project として開き、`.codex/config.toml` が読み込まれることを確認する。
- 必要なら `./.codex/hooks/session-end.sh` で通知動作を手元確認する。
